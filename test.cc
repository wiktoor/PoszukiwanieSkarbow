#include <bits/stdc++.h>
#include "treasure.h"
using namespace std;

int main() {
    static_assert(numeric_limits<int16_t>::is_integer);

    int x = 5;
    Treasure t(x, true);

    long long z = 5;
    Treasure(z, true);

    //double y = 5;
    //Treasure(y, false);
}